# 0.3.0-alpha.2 本地候选验证

日期：2026-09-06。未公开发布、未覆盖安装目录。此前未交付的 alpha.2 包已在项目内归档，本次追加活动列表导航修复。

## 已确认的用户反馈

- alpha.1 启动、语音桥连接、真实电量与增强按键三步校准成功。
- 用户确认返回 Delete、菜单 Typeless Translate 基本正常。
- 聚焦 Codex 输入框后，旧音量映射能够切换任务，但顺序与活动视图显示不符。不能将此记录为活动视图验收通过。
- 最终 alpha.2 用户实测通过：音量－按活动视图下移一项，音量＋回到原任务，顺序正确；新版返回/Delete 与菜单/Typeless Translate 均正常。日志同时记录 Activity 上一/下一任务、返回 Delete 和菜单 Typeless 翻译动作。

## 本次修复

- 活动视图通过 Windows 可访问性接口直接选择当前已加载列表的相邻任务行；按实际渲染顺序移动，到首尾停止，不修改 Codex 快捷键。
- 任务菜单打开、当前行不明确、列表变化、焦点或修饰键状态变化时跳过。已识别活动视图但无法定位时，不回退到不同排序的默认快捷键。
- 普通视图保留 Ctrl+PageUp/PageDown，改为一次完整四事件提交；部分提交失败时只释放已接受且尚未松开的键。
- 保留此前的暂停/安全退出立即撤销动作、暂停后先恢复连接、重新连接替换故障控制器等修复。

## 检查记录

- 普通导航发送离线回归：27 项通过，0 失败，0 次原生调用。覆盖两个方向、全部部分发送前缀、释放重试与失败后继续释放、取消前后清理；未把前台身份与实体修饰键检查计入动态覆盖。
- 本机 Codex 26.901.5280.0 的只读 UI 检查确认活动列表可枚举已加载任务行及唯一当前行。代码核对确认菜单高亮可能复用当前行样式，因此实现必须拒绝打开菜单时导航。
- 辅助组件未改动，沿用此前 Python 19 项、JavaScript 解析/租约、真实组件连接/安全退出证据。
- 修改后的最终编译、打包程序集检查、实际活动列表上下切换与暂停恢复结果在下方续记。不得沿用此前 alpha.2 包的校验值。

## 实机与交付待办

2026-09-06 00:25 后，当前已切换为最终 alpha.2 便携包，并完成语音连接与增强按键校准。用户已确认活动视图音量－下移一项、音量＋回到原任务，顺序正确；返回/Delete 与菜单/Typeless Translate 回归也通过。暂停恢复、语音实际输入、完整翻译输出、安装升级、登录启动、驱动断线重连和卸载的完整验收仍未完成；语音桥报告 `armed` 仅表示就绪，不代表实际语音输入已通过。

最终产物位于 artifacts/release 的 0.3.0-alpha.2 安装包与便携目录。本次以前的候选保留在 artifacts/build/0.3.0-alpha.2/before-activity-navigation-fix；安装目录与 alpha.1 产物未改动，alpha.1 运行实例已通过自身的安全退出流程结束。

## 本次最终构建检查

- 修订版 Release 编译通过，0 警告、0 错误；新包通过 4 条合法与 15 条畸形协议消息检查。
- 最终包实际 WinForms 六状态检查全部通过，检查模式未初始化硬件或争用运行实例。
- 活动导航代码完成独立审查：修复隐藏面板可能回退错误排序的分支；可见面板两次确认无活动列表后才允许普通快捷键。
- 安装包和便携 ZIP 已重新生成。此处的构建检查不代表实际音量键切换已经验收。
- 首次只读实机检查时，Codex 未处于目标活动视图/前台，因此该轮未完成且没有触发任务切换；当时旧 alpha.1 仍在运行。后续只读检查及版本切换结果见下节。
- 最终校验通过：367 个 payload 文件及 2 个发布文件 SHA-256 全部匹配。

## 2026-09-06 00:24–00:25 实机续记

- 00:24 后，生产实现的只读 `CaptureSnapshot` 成功：已加载任务行数 `20`，当前索引 `1`，上/下方向均可导航，耗时 `293 ms`。这是列表识别与可导航性检查，没有调用任务切换，不能代替实体音量键验收。
- 通过 alpha.1 自身的“安全退出”结束旧实例；确认 Tray、GattProbe、KeyBridge 均无残留后，启动最终 alpha.2 便携包，Tray 进程为 PID `34016`。
- 00:25，UI Automation 读取到“已连接”、真实电量 `60%`、“增强已启用”；用户已完成三键校准。辅助组件管道身份认证成功，语音桥报告 `armed`。
- 该时段用户开始执行音量－下移、音量＋返回原任务，以及返回/Delete、菜单/Translate 回归；当时反馈待收集，后续确认见下节。
- 最终检查汇总：27 项普通导航离线回归、19 条协议消息检查、6 种 UI 控制状态检查通过；367 个 payload 文件及 2 个发布文件 SHA-256 匹配。此前 19 项 Python 辅助组件测试证据保留，和 19 条协议消息检查分开计数。

## 最终 alpha.2 用户实测确认

- 用户确认活动视图音量－下移一项、音量＋回到原任务，切换顺序与眼前列表一致。
- 用户确认新版返回/Delete 与菜单/Typeless Translate 均正常。
- alpha.2 运行日志与反馈相符，记录了 Activity 上一/下一任务、返回 Delete 和菜单 Typeless 翻译动作。
- 本轮通过的是活动列表相邻项往返导航，以及返回和翻译入口回归；未据此宣布完整语音输入、翻译输出、暂停恢复或安装生命周期通过。
- 本次只追加验证记录，未修改构建产物；已核验的 367 个 payload 文件与 2 个发布文件校验结果保持不变。
