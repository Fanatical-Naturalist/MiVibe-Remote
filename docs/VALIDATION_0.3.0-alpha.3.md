# 0.3.0-alpha.3 验证记录

日期：2026-09-06。

- 用户要求把返回键从 Delete 改为 Backspace，Home 继续 Delete；TV 仅询问可改性，未指定新动作。
- alpha.3 使用扫描码 0x0E 发出 Backspace，保留单次按下、修饰键守卫、统一动作队列与完整释放。Home 仍使用扩展扫描码 0x53 的 Delete。
- 原音量活动视图导航与菜单翻译已在 alpha.2 实测通过，本次未修改。
- 安装、开始菜单固定、开机启动及 GitHub 发布继续沿用此前用户授权。
- 构建、安装及返回/Home 实测结果在下方续记；不将先前 Delete 的验收当作 Backspace 验收。

## 最终本机验收

- Release 编译通过，0 警告、0 错误。最终包通过 4 条合法与 15 条畸形通信消息检查。
- Setup 从现有安装升级至 alpha.3，退出码 0，不需要重启。正式目录为 E:\Program Files\MiVibe Remote；368 个安装文件与发行包校验值一致，原系统扫描码映射保持不变。
- 当前运行的是正式安装目录中的 alpha.3，语音桥已连接、增强按键已校准启用。
- 用户确认返回键删除光标前字符、Home 删除光标后字符，两键长按都只触发一次。
- 开始菜单固定通过可见右键菜单完成，随后“取消固定”状态验证成功。开机启动指向正式安装目录，未发现 Windows StartupApproved 禁用覆盖。
- 重复启动带 --show-status-window 的入口后，新启动器正常退出，原进程保持唯一且控制中心重新显示。
- GitHub 安装包、便携 ZIP、校验文件三项远端摘要与本地文件一致；构建对应源码提交 388e3fa03c9b7035143efb120826ca67e35a1db2。
- 此次只补充验收记录，不改动已核验的发行文件。登录后的真实启动、驱动重连、完整语音输入及卸载仍不以本轮检查替代。