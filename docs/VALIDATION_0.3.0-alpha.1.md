# 0.3.0-alpha.1 本地验证记录

日期：2026-09-05。当前为本地预览候选，未公开发布，也未覆盖安装目录。

## 已完成

- .NET 9.0.317 Release 构建：0 警告、0 错误。
- Python 单元测试 19 项通过：校准顺序、同流识别、长按去重、重连撤销、取消附加、独立清理令牌、队列上限及父进程/管道断开。
- JavaScript 模拟测试通过：同步九字节报告、无效/未知 usage、句柄关闭、租约过期。没有把异步完成路径算作支持。
- 最终程序集协议检查通过：4 种合法消息和 15 种畸形消息。
- 打包后的只读组件真实进程测试：非管理员返回需要管理员权限后退出；管理员运行进入 connecting → calibrating，10 秒内收到 6 个心跳，未触发按键动作，安全退出码 0。
- 实际 WinForms 明暗主题分别渲染已启用、校准中、未启用状态；修复了当前 144 DPI 下标题与连接说明的裁切。图中的电量与连接状态明确为界面示例。
- 自包含 win-x64 便携 ZIP 与 Inno Setup 安装包生成成功；323 个 payload 文件 SHA-256 全部匹配。
- 仓库改动通过 `git diff --check`；原始设备报告与构建依赖保持在忽略目录。

## 产物

- `artifacts/release/MiVibe-Remote-Setup-0.3.0-alpha.1-win-x64.exe`
- `artifacts/release/MiVibe-Remote-0.3.0-alpha.1-win-x64.zip`
- `artifacts/release/MiVibe-Remote-0.3.0-alpha.1-SHA256SUMS.txt`
- `artifacts/ui-v03/v03-dark-active.png` 等六张实际程序渲染图。

## 尚需整机验收

本记录生成时旧版仍从 `E:\Program Files\MiVibe Remote` 运行。已请求用户先通过旧版“安全退出”释放单实例与语音连接，再启动 0.3。尚未把旧版按键探针的成功当作新包的最终验收。

待在 0.3 主程序中完成启用/校准、Delete 与侧栏切换、菜单翻译、语音并行使用、暂停/恢复及安全退出；之后再验证驱动断线恢复、登录启动、覆盖安装和卸载。

## 2026-09-05 主程序整机验证（进行中）

- 用户通过旧版安全退出后确认退出；只读进程检查未发现旧版残留。
- 从0.3便携目录启动主程序。语音桥成功进入 armed，电量从设备真实读取为61%；主程序、语音桥均来自0.3目录。
- 主程序成功启动增强组件并通过管道身份验证，状态进入calibrating。
- 用户完成返回→音量＋→音量－校准，明确确认“已启用，三个步骤都完成了”；UI自动化只读检查与用户反馈一致。
- 实际已校准窗口截图保存在 artifacts/ui-v03/v03-live-calibrated.png；此图为真实运行状态。
- Delete、音量侧栏导航、Translate与麦克风并行使用的用户回归反馈仍在等待中。
